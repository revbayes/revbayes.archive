# Test file for debugging

# a <- 1 + 2

# b <- dexp(1, 1)

# a <- vallist( a=1, b=true )

# a <- read( "replicase.nex" )

a <- 2   # caused an error before because of the comment

x   124

b <- does_not_exist   # this should cause an error

c <- 4   # this should not be reached
