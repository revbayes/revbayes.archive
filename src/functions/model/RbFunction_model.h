#ifndef RbFunction_H
#define RbFunction_H

#include <set>
#include "DAGNode.h"

namespace RbFunction {

/*Model* createModel(std::set<DAGNode*> &sinkNodes) {

	Model *m = new Model(sinkNodes);
	if ( !m->completeModel() )
		return NULL;
	return m;
}*/

}

#endif